<div class="sidebar">
    <a href="/settings"><h1 class="sidebar_label">Settings</h1></a>
    <a href="/settings/appearance" class="sidebar_element">Appearance</a>
    <a href="/settings/gpio" class="sidebar_element">GPIO</a>
    <a href="settings/camera" class="sidebar_element">Camera</a>
    <a href="/settings/audio" class="sidebar_element">Audio</a>
    <a href="/" class="sidebar_element_exit"><img src="icons/arrow-left.png" class="back_icon" alt="Back icon">Go back to Control Panel</a>
</div>
<style>
    img.back_icon {
        margin-left: 12px;
        margin-right: 12px;
    }
    /* exit button */
    .sidebar a.sidebar_element:hover {
        color: #b0b0b0;
    }

    .sidebar a.sidebar_element_exit:hover {
        color: #b0b0b0;
    }
    .sidebar a.sidebar_element_exit {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        margin-top: 50px;
        font-size: 25px;
        font-family: 'Montserrat', sans-serif;
        color: #000000;
        display: block;
    }
    /* main styles */
    .sidebar {
        width: 250px; /* Set the width of the sidebar */
        position: fixed; /* Fixed Sidebar (stay in place on scroll) */
        z-index: 1; /* Stay on top */
        top: 0; /* Stay at the top */
        left: 0;
        border-radius: 12px;
        background-color: #fafa3499; /* Black */
        overflow-x: hidden; /* Disable horizontal scroll */
        padding-top: 20px;
        padding-bottom: 20px;
        margin: 24px;
    }

    /* The sidebar menu */
    h1.sidebar_label {
        font-size: 50px;
        font-family: 'Montserrat', sans-serif;
        padding: 6px 8px 6px 16px;
        color: black;
        text-decoration: none;
        display: block;
    }

    .sidebar a.sidebar_element {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        font-size: 25px;
        font-family: 'Montserrat', sans-serif;
        color: #000000;
        display: block;
    }

    .sidebar a.sidebar_element_exit {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        margin-top: 50px;
        font-size: 25px;
        font-family: 'Montserrat', sans-serif;
        color: #000000;
        display: block;
    }
</style>